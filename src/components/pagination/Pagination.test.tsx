import React from 'react';

describe('Header component', () => {
  test('should render left arrow', () => {
    expect(1).toEqual(2);
  });

  test('should render right arrow', () => {
    expect(1).toEqual(2);
  });

  test('should render page number', () => {
    expect(1).toEqual(2);
  });

  test('should set next page on click', () => {
    expect(1).toEqual(2);
  });

  test('should set previous page on click', () => {
    expect(1).toEqual(2);
  });

  test('should set page on pahe number click', () => {
    expect(1).toEqual(2);
  });
});
