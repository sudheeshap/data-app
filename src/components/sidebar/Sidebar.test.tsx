import React from 'react';

describe('Sidebar component', () => {
  it('should render button', () => {
    expect(1).toEqual(2);
  });

  it('should render expand toggle', () => {
    expect(1).toEqual(2);
  });
});
